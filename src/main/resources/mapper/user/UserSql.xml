<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.user.mapper.UserMapper">

    <!-- 로그인: 이메일로 사용자 조회 -->
    <select id="getUserByEmail" parameterType="String" resultType="com.example.demo.dto.UserDto">
        SELECT id, email, password
        FROM user
        WHERE email = #{email}
    </select>

    <!-- 사용자 ID로 Salt 조회 -->
    <select id="getSaltByUserId" parameterType="int" resultType="String">
        SELECT salt FROM user_salt WHERE user_id = #{userId}
    </select>

    <!-- 모든 유저 조회 -->
    <select id="getAllUsers" resultType="com.example.demo.dto.UserDto">
        SELECT id, email, nickname, profile_image 
        FROM user
    </select>

    <!-- 닉네임으로 유저 조회 -->
    <select id="searchUsersByNickname" resultType="com.example.demo.dto.UserDto">
        SELECT id, email, nickname, profile_image, my_intro
        FROM user
        WHERE nickname LIKE CONCAT('%', #{nickname}, '%')
    </select>

    <select id="getUserById" parameterType="int" resultType="com.example.demo.dto.UserDto">
        SELECT id, nickname, profile_image, my_intro
        FROM user
        WHERE id = #{id}
    </select>

    <!-- 유저 삽입 -->
	<insert id="insertUser" parameterType="com.example.demo.dto.UserDto">
	    INSERT INTO user (email, password, nickname)
	    VALUES (#{email}, #{password}, #{nickname});
	    <selectKey resultType="int" keyProperty="id" order="AFTER">
	        SELECT LAST_INSERT_ID()
	    </selectKey>
	</insert>
	
	<!-- salt 삽입 -->
	<insert id="insertUserSalt">
	    INSERT INTO user_salt (user_id, salt)
	    VALUES (#{userId}, #{salt})
	</insert>

    <!-- 유저 삭제 -->
    <delete id="deleteUser" parameterType="int">
        DELETE FROM user WHERE id = #{id}
    </delete>

</mapper>
