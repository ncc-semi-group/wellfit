<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.board.mapper.UserMapper">

    <!-- 사용자 추가 -->
    <insert id="insertUser" parameterType="com.example.demo.dto.UserDto" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (
            email, password, nickname, profile_image, gender, age, height, activity_level, 
            current_weight, goal_weight, cheating_day, type, carbohydrate, protein, fat, created_at, my_intro
        )
        VALUES (
            #{email}, #{password}, #{nickname}, #{profileImage}, #{gender}, #{age}, #{height}, 
            #{activityLevel}, #{currentWeight}, #{goalWeight}, #{cheatingDay}, #{type}, 
            #{carbohydrate}, #{protein}, #{fat}, #{createdAt}, #{myIntro}
        )
    </insert>

    <!-- 사용자 정보 조회 -->
    <select id="selectUserById" parameterType="int" resultType="com.example.demo.dto.UserDto">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <!-- 이메일로 사용자 조회 -->
    <select id="selectUserByEmail" parameterType="String" resultType="com.example.demo.dto.UserDto">
        SELECT * FROM users WHERE email = #{email}
    </select>

    <!-- 사용자 정보 업데이트 -->
    <update id="updateUser" parameterType="com.example.demo.dto.UserDto">
        UPDATE users
        SET 
            email = #{email}, password = #{password}, nickname = #{nickname}, profile_image = #{profileImage}, 
            gender = #{gender}, age = #{age}, height = #{height}, activity_level = #{activityLevel}, 
            current_weight = #{currentWeight}, goal_weight = #{goalWeight}, cheating_day = #{cheatingDay}, 
            type = #{type}, carbohydrate = #{carbohydrate}, protein = #{protein}, fat = #{fat}, 
            created_at = #{createdAt}, my_intro = #{myIntro}
        WHERE id = #{id}
    </update>

    <!-- 사용자 목록 조회 -->
    <select id="selectAllUsers" resultType="com.example.demo.dto.UserDto">
        SELECT * FROM users
    </select>

    <!-- 사용자 삭제 -->
    <delete id="deleteUser" parameterType="int">
        DELETE FROM users WHERE id = #{id}
    </delete>

</mapper>
